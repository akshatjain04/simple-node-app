# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /example_get for http method type GET in artillery framework
# RoostTestHash=2d7889707c



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "example_get_others.csv"
    fields:
      - "invalidId"
      - "invalidToken"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Response Test"
    flow:
      - post:
          url: "/example"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            invalidField: "invalidData"
          expect:
            - statusCode: 401
            - hasHeader: "www-authenticate"
      - think: 1

      - post:
          url: "/example"
          json:
            validField: "validData"
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
      - think: 1

      - post:
          url: "/example"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            invalidField: "invalidData"
          expect:
            - statusCode: 422
            - hasProperty: "error"
      - think: 1

      - get:
          url: "/invalidExample"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

      - delete:
          url: "/example"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 405
            - hasHeader: "allow"
      - think: 1
