# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /ping_get for http method type GET in artillery framework
# RoostTestHash=628b0897a0



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "ping_get_success.csv"
    fields:
      - "pingId"
      - "pingMessage"
    skipHeader: true
  defaults:
    headers:
      Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
  plugins:
    expect: {}

scenarios:
  - name: "API Success Response Scenario"
    flow:
      - get:
          url: "/ping"
          expect:
            - statusCode: 200
            - hasHeader: "Content-Type"
      - think: 1
