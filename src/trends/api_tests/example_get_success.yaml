# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /example_get for http method type GET in artillery framework
# RoostTestHash=2d7889707c



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "example_get_success.csv"
    fields:
      - "field1"
      - "field2"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API Responses"
    flow:
      - get:
          url: "/example?paramName={{ field1 }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: 'Content-Type'
            - contentType: 'application/json'
            - hasProperty: 'property1'
      - think: 1
      - post:
          url: "/example"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            key1: "{{ field1 }}"
            key2: "{{ field2 }}"
          expect:
            - statusCode: 201
            - hasHeader: 'Content-Type'
            - contentType: 'application/json'
            - hasProperty: 'property2'
      - think: 1
      - put:
          url: "/example"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            key1: "{{ field1 }}"
            key2: "{{ field2 }}"
          expect:
            - statusCode: 200
            - hasHeader: 'Content-Type'
            - contentType: 'application/json'
            - hasProperty: 'property3'
      - think: 1
      - delete:
          url: "/example?paramName={{ field2 }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 204
            - hasHeader: 'Content-Type'
            - contentType: 'application/json'
      - think: 1
